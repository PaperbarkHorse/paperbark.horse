---

---

<div class="album-grid">
    <slot />
</div>

<style lang="scss">
    .album-grid {
        --content-gap: 2em;

        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(9rem, 1fr));

        gap: 1rem;
    }
</style>
