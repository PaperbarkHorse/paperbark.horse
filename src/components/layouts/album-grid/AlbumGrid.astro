---

---

<div class="album-grid">
    <slot />
</div>

<style lang="scss">
    .album-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(9rem, 1fr));

        gap: 1rem;
    }
</style>

<style lang="scss" is:global>
    .content + .album-grid {
        margin-top: 1rem;
    }
</style>
